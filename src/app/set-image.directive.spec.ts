import { SetImageDirective } from './set-image.directive';

describe('SetImageDirective', () => {
  it('should create an instance', () => {
    const directive = new SetImageDirective();
    expect(directive).toBeTruthy();
  });
});
